import { FlightdetailsDirective } from './flightdetails.directive';

describe('FlightdetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new FlightdetailsDirective();
    expect(directive).toBeTruthy();
  });
});
